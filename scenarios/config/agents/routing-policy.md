Route each task through the lowest sufficient layer: deterministic structured tooling,
then bounded JEV for sanitized ambiguous candidates, then normal agent reasoning, and
only then an evidence-backed isolated custom agent. Do not load broad context to obtain
facts a deterministic tool already supplies.
