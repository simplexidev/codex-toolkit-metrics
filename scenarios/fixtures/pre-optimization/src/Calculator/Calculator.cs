namespace Baseline;

public static class Calculator
{
    public static int Add(int left, int right) => left + right;

    public static int Divide(int numerator, int denominator) => numerator / denominator;
}

